import { ExperimentForm } from "./form"; import { PageHeader } from "@/components/layout/page-header"; export const metadata = { title: "New prompt experiment" }; export default function NewExperimentPage() { return <div className="space-y-6"><PageHeader title="New prompt experiment" description="The backend executes every case and selects the winner. The browser submits no calculated metrics." /><ExperimentForm /></div>; }
