import { redirect } from "next/navigation"; import { getSession } from "@/lib/auth/session"; import { MediaAssetDetail } from "./view"; export const metadata = { title: "Media asset" }; export default async function MediaAssetPage({ params }: { params: Promise<{ assetId: string }> }) { const [{ assetId }, session] = await Promise.all([params, getSession()]); if (!session) redirect("/login"); return <MediaAssetDetail assetId={assetId} role={session.role} />; }
