import { ImageIcon, Plus, Video } from "lucide-react";
import Link from "next/link";
import { PageHeader } from "@/components/layout/page-header";
import { Button } from "@/components/ui/button";
import { MediaHistory } from "./view";
export const metadata = { title: "Media studio" };
export default function MediaPage() { return <div className="space-y-6"><PageHeader title="Media studio" description="Generate review-gated images and structured video storyboards. Publishing remains outside the product." actions={<><Button asChild><Link href="/media/images/new"><ImageIcon className="size-4" />Generate image</Link></Button><Button asChild variant="secondary"><Link href="/media/storyboards/new"><Video className="size-4" />New storyboard</Link></Button></>} /><div className="grid gap-px border bg-[var(--border)] md:grid-cols-2"><section className="bg-white p-6"><ImageIcon className="size-6 text-[var(--accent)]" /><h2 className="mt-4 font-semibold">Image generation</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">Create a safe media request, follow generation status, inspect the stored asset, and record a human review.</p><Button asChild size="sm" className="mt-5"><Link href="/media/images/new"><Plus className="size-4" />New image</Link></Button></section><section className="bg-white p-6"><Video className="size-6 text-[var(--info)]" /><h2 className="mt-4 font-semibold">Video storyboard</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">Generate a readable scene sequence with voice-over, on-screen text, timing, and provider traceability.</p><Button asChild size="sm" variant="secondary" className="mt-5"><Link href="/media/storyboards/new"><Plus className="size-4" />New storyboard</Link></Button></section></div><MediaHistory /></div>; }
