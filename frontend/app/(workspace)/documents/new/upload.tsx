"use client";
import { useMutation } from "@tanstack/react-query"; import { useRouter } from "next/navigation"; import { toast } from "sonner"; import { FileUploadForm } from "@/components/forms/file-upload-form"; import { documentsApi } from "@/lib/api/documents"; import { isApiError } from "@/lib/api/errors";
export function DocumentUpload() { const router = useRouter(); const mutation = useMutation({ mutationFn: documentsApi.create, onSuccess: (task) => router.push(`/documents/${task.task_run_id}`), onError: (error) => toast.error(isApiError(error) ? error.message : "Upload failed") }); return <FileUploadForm accept=".pdf,.docx,.txt,application/pdf,text/plain,application/vnd.openxmlformats-officedocument.wordprocessingml.document" label="Source document" description="The server verifies MIME type, file size, extraction limits, and unsafe prompt-like content." pending={mutation.isPending} onSubmit={(file) => mutation.mutate(file)} />; }
